# Variables de entorno - Template
# Copia este archivo a .env y rellena los valores según tu entorno

# Base de datos
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_NAME=sistema_ventas

# JWT
JWT_SECRET=CLAVE_SECRETA_EXAMEN

# (Opcional) Para XAMPP local, deja DB_PASSWORD vacío (sin valor)
# Para Docker, usa: DB_PASSWORD=rootpwd
